# 🎛️ دليل اختبار لوحة تحكم المشغل

## طريقتان للوصول لـ operator-dashboard.html

---

## ✅ الطريقة 1: تجاوز تسجيل الدخول (الأسرع)

### الخطوات:

1. **افتح صفحة لوحة التحكم:**
   ```
   http://localhost:8000/operator-dashboard.html
   ```

2. **افتح Console (F12)**

3. **الصق هذا الكود:**
   ```javascript
   localStorage.setItem('user', JSON.stringify({
       id: 'test-operator-id',
       email: 'operator@karam.sa',
       role: 'operator',
       full_name: 'مشغل المنصة'
   }));
   location.reload();
   ```

4. **اضغط Enter**

**✅ النتيجة:** ستُحمّل لوحة التحكم مباشرة!

---

## ✅ الطريقة 2: إنشاء مستخدم حقيقي

### في Supabase:

#### **1. إنشاء Auth User:**

في Supabase Dashboard:
- اذهب إلى **Authentication** → **Users**
- اضغط **Add user**
- **Email:** `operator@karam.sa`
- **Password:** `operator123`
- **Confirm:** اضغط **Create user**

#### **2. احصل على User ID:**

```sql
SELECT id FROM auth.users WHERE email = 'operator@karam.sa';
```

#### **3. إنشاء User Profile:**

```sql
-- استبدل YOUR_USER_ID بالـ ID من الخطوة السابقة
INSERT INTO user_profiles (id, user_type, full_name)
VALUES (
    'YOUR_USER_ID',
    'operator',
    'مشغل المنصة'
);
```

#### **4. تسجيل الدخول:**

- افتح `http://localhost:8000/login.html`
- **Email:** `operator@karam.sa`
- **Password:** `operator123`
- اضغط **تسجيل الدخول**

**✅ سيتم توجيهك تلقائياً إلى `operator-dashboard.html`**

---

## 📋 ما يمكنك فعله في لوحة التحكم:

### **القسم 1: نظرة عامة**
- إحصائيات المنصة
- الحجوزات الحالية
- الإيرادات

### **القسم 2: إدارة الأسر**
- عرض الأسر المنتظرة
- الموافقة على الأسر الجديدة
- تعطيل/تفعيل الأسر

### **القسم 3: إدارة الأسعار**
- الباقات
- قواعد التسعير الديناميكي

### **القسم 4: التقارير المالية**
- الإيرادات
- تقارير الأسر

### **القسم 5: إعدادات المنصة**
- رسوم المنصة
- الإعدادات العامة

---

## 🔧 استكشاف الأخطاء

### **لوحة التحكم لا تظهر:**

1. **تحقق من Console:**
   - اضغط `F12`
   - ابحث عن أخطاء حمراء

2. **تحقق من localStorage:**
   ```javascript
   console.log(localStorage.getItem('user'));
   ```

3. **تحقق من الدور (role):**
   ```javascript
   const user = JSON.parse(localStorage.getItem('user'));
   console.log(user.role); // يجب أن يكون 'operator'
   ```

### **الصفحة تُعيد التوجيه:**

- تأكد أن `role` هو `'operator'` وليس `'umrah_visitor'`

---

## 📌 ملاحظة مهمة

**الطريقة 1 (localStorage)** مناسبة فقط للاختبار المحلي.  
**الطريقة 2 (Auth User)** مناسبة للإنتاج.

---

## 🚀 جاهز للاختبار!

استخدم **الطريقة 1** للوصول السريع الآن! ✅
