# دليل الإعداد السريع لقاعدة البيانات

## ✅ الخطوة 1: إنشاء مشروع Supabase

### 1.1 افتح المتصفح وانتقل إلى:
```
https://supabase.com
```

### 1.2 سجّل دخول:
- استخدم حساب GitHub
- أو أنشئ حساب جديد

### 1.3 إنشاء مشروع جديد:
1. اضغط **"New Project"**
2. املأ البيانات:
   - **Name:** `karam-platform`
   - **Database Password:** اختر كلمة مرور قوية (احفظها!)
     - مثال: `Karam@2025#Secure`
   - **Region:** اختر `Frankfurt` (الأقرب للسعودية)
3. اضغط **"Create new project"**
4. انتظر 2-3 دقائق حتى يجهز المشروع

---

## ✅ الخطوة 2: تنفيذ SQL Schema

### 2.1 افتح SQL Editor:
- من القائمة اليسرى: **SQL Editor**
- اضغط **"+ New query"**

### 2.2 نفّذ Schema الأساسي:
1. افتح ملف: `C:\Users\Shakir\.gemini\antigravity\scratch\karam-platform\database\complete_schema.sql`
2. اضغط `Ctrl+A` لتحديد الكل
3. اضغط `Ctrl+C` للنسخ
4. ارجع لـ Supabase SQL Editor
5. الصق المحتوى `Ctrl+V`
6. اضغط **"Run"** أو `F5`
7. انتظر حتى تظهر "Success ✓"

### 2.3 (اختياري) نفّذ نظام Time Slots:
1. افتح ملف: `database\availability_system.sql`
2. كرر نفس الخطوات أعلاه
3. شغّل الـ SQL

---

## ✅ الخطوة 3: نسخ مفاتيح API

### 3.1 اذهب للإعدادات:
- من القائمة اليسرى: **Settings** → **API**

### 3.2 انسخ المفاتيح التالية:

**Project URL:**
```
https://xxxxxxxxxxxxx.supabase.co
```

**anon public key:**
```
eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOi...
```

---

## ✅ الخطوة 4: تحديث المشروع

### 4.1 افتح ملف `js/main.js`:

ابحث عن السطر 6-7 وعدّلهم:
```javascript
// قبل:
const API_BASE_URL = 'https://placeholder.supabase.co';
const API_KEY = 'your-anon-key';

// بعد (استبدل بالقيم الحقيقية):
const API_BASE_URL = 'https://xxxxxxxxxxxxx.supabase.co';
const API_KEY = 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOi...';
```

### 4.2 افتح ملف `js/auth.js`:

ابحث عن السطر 8-9 وعدّلهم:
```javascript
// قبل:
const SUPABASE_URL = 'https://placeholder.supabase.co';
const SUPABASE_ANON_KEY = 'your-anon-key';

// بعد (استبدل بالقيم الحقيقية):
const SUPABASE_URL = 'https://xxxxxxxxxxxxx.supabase.co';
const SUPABASE_ANON_KEY = 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOi...';
```

### 4.3 احفظ الملفات:
- اضغط `Ctrl+S` في كل ملف

---

## ✅ الخطوة 5: التجربة!

### 5.1 أعد تحميل الصفحة:
- في المتصفح المفتوح على `http://localhost:8000`
- اضغط `F5` أو `Ctrl+R`

### 5.2 جرّب التسجيل:
1. اضغط **"التسجيل"**
2. اختر **"معتمر"**
3. املأ البيانات:
   - الاسم: `أحمد محمد`
   - البريد: `test@example.com`
   - الجوال: `0501234567`
   - كلمة المرور: `Test@123`
4. اضغط **"إنشاء حساب"**

### 5.3 تحقق من النجاح:
- إذا ظهرت رسالة "تم التسجيل بنجاح" → ✅ كل شيء يعمل!
- إذا ظهر خطأ → ارجع للخطوة 4 وتأكد من المفاتيح

---

## 🎯 إضافة بيانات تجريبية (اختياري)

لإضافة أسرة تجريبية للتصفح:

### في Supabase SQL Editor:
```sql
-- إضافة مستخدم الأسرة
INSERT INTO user_profiles (email, user_type, full_name, phone) 
VALUES ('family@test.com', 'family', 'أسرة آل سعيد', '0509876543')
RETURNING id;

-- احفظ الـ ID وضعه في الاستعلام التالي
INSERT INTO host_families (user_id, family_name, city, address, capacity, description, status)
VALUES (
    'الصق_الـID_هنا',
    'أسرة آل سعيد',
    'makkah',
    'حي العزيزية - مكة المكرمة',
    15,
    'أسرة كريمة ترحب بضيوف الرحمن في مجلس أصيل',
    'approved'
);
```

الآن أعد تحميل صفحة التصفح - ستجد الأسرة!

---

## ❓ استكشاف الأخطاء

### مشكلة: "Failed to fetch"
**الحل:** تحقق من:
- ✅ المفاتيح صحيحة في `main.js` و `auth.js`
- ✅ URL لا ينتهي بـ `/`
- ✅ Internet متصل

### مشكلة: "User already registered"
**الحل:** 
- استخدم email مختلف
- أو سجّل دخول بنفس Email

### مشكلة: لا تظهر أسر
**الحل:**
- أضف أسرة تجريبية من SQL Editor
- تأكد من `status = 'approved'`

---

## 🎉 تم!

بعد إتمام هذه الخطوات، المشروع سيعمل بالكامل مع:
- ✅ التسجيل والدخول
- ✅ تصفح الأسر
- ✅ الحجوزات
- ✅ لوحات التحكم

**أخبرني عند أي خطوة لمساعدتك بالتفصيل!** 🚀
